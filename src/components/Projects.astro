---
---

<section id="projects" class="min-h-screen flex items-center py-32 px-6 border-t-4 border-[var(--accent)] bg-[var(--bg-secondary)]">
  <div class="max-w-3xl mx-auto w-full">
    <!-- Section header -->
    <div class="mb-20">
      <span class="mono text-sm bg-[var(--accent)] text-[var(--bg-primary)] px-4 py-2 inline-block mb-8">02 — WORK</span>
      <h2 class="text-5xl md:text-7xl font-bold text-[var(--text-primary)]">PROJECTS</h2>
    </div>

    <!-- Project list -->
    <div class="space-y-12">
      <!-- Sake -->
      <article class="p-8 md:p-10 bg-[var(--bg-primary)] border-2 border-[var(--accent)]">
        <div class="flex items-center justify-between mb-6">
          <h3 class="text-2xl md:text-3xl font-bold text-[var(--text-primary)] flex items-center gap-3">
            <span>🍶</span> Sake
          </h3>
          <span class="mono text-xs px-3 py-1 bg-[var(--accent)] text-[var(--bg-primary)]">ACTIVE</span>
        </div>

        <p class="text-lg text-[var(--text-secondary)] mb-8">
          A lightweight, high-performance web framework for Navi.<br/>
          45,000+ requests per second.
        </p>

        <div class="flex gap-4">
          <a href="https://ryancooper-dev.github.io/sake-navi/" target="_blank"
             class="mono text-sm px-6 py-3 bg-[var(--accent)] text-[var(--bg-primary)]">
            DOCS →
          </a>
          <a href="https://github.com/ryancooper-dev/sake-navi" target="_blank"
             class="mono text-sm px-6 py-3 border-2 border-[var(--accent)] text-[var(--accent)]">
            GITHUB →
          </a>
        </div>
      </article>

      <!-- Agentries -->
      <article class="p-8 md:p-10 bg-[var(--bg-primary)] border-2 border-[var(--border)] opacity-60">
        <div class="flex items-center justify-between mb-6">
          <h3 class="text-2xl md:text-3xl font-bold text-[var(--text-primary)] flex items-center gap-3">
            <span>🤖</span> Agentries
          </h3>
          <span class="mono text-xs px-3 py-1 border border-[var(--text-tertiary)] text-[var(--text-tertiary)]">PRIVATE</span>
        </div>

        <p class="text-lg text-[var(--text-secondary)]">
          Enterprise-grade AI agent infrastructure built in Rust.
        </p>
      </article>
    </div>
  </div>
</section>
